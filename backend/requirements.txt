# Web framework
# fastapi>=0.115.3 pulls starlette>=0.40 (CVE-2024-47874: unbounded multipart
# memory DoS, reachable via /api/agent/upload-report and /api/chat/transcribe).
fastapi==0.115.6
uvicorn[standard]==0.30.0

# Database
supabase==2.7.0
asyncpg==0.29.0
sqlalchemy==2.0.36
alembic==1.13.3

# AI
anthropic==0.40.0
openai==1.54.0          # Whisper STT for mobile voice input (optional)

# QuickBooks
conductor-api==2.1.0
# python-quickbooks — optional, requires Rust toolchain; install separately if needed
# intuitlib not on PyPI — OAuth handled natively via httpx

# Background tasks
celery==5.4.0
redis==5.1.0

# Auth & Security
python-jose[cryptography]==3.4.0   # 3.3.0: CVE-2024-33663 / CVE-2024-33664 (JWT bomb)
passlib[bcrypt]==1.7.4
pyotp==2.9.0
cryptography==43.0.3

# HTTP & OAuth
httpx==0.27.2
authlib==1.3.2
python-multipart==0.0.18           # 0.0.12: CVE-2024-53981 (boundary DoS)

# Email
sendgrid==6.11.0

# Billing (billing.py runs in MOCK mode if this is missing)
stripe==11.2.0

# File processing
pypdf==5.1.0                       # replaces unmaintained pypdf2 3.0.1 (CVE-2023-36464)
openpyxl==3.1.5
pandas==2.2.3
watchfiles==0.24.0

# Browser automation (headless portal workflows)
# Run `playwright install chromium` after pip install
playwright==1.48.0

# Google APIs (Gmail + Sheets)
google-auth==2.36.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.153.0

# Computer Use — desktop-only; install in Tauri companion process, NOT the cloud API
# pyautogui==0.9.54
# mss==9.0.2
Pillow==11.0.0              # Image processing (used server-side for resize too)
# pytesseract==0.3.13       # OCR — optional, requires system tesseract

# Config
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.14.2

# Monitoring
sentry-sdk[fastapi]==2.18.0
python-json-logger==2.0.7

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
