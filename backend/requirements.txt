# Web framework
# fastapi>=0.115.3 pulls starlette>=0.40 (CVE-2024-47874: unbounded multipart
# memory DoS, reachable via /api/agent/upload-report and /api/chat/transcribe).
fastapi==0.141.1
uvicorn[standard]==0.52.4

# Database
supabase==2.31.0
asyncpg==0.31.0
sqlalchemy==2.0.52
alembic==1.19.2

# AI
anthropic==0.125.0
openai==1.109.1          # Whisper STT for mobile voice input (optional)

# QuickBooks
conductor-api==2.1.0
# python-quickbooks — optional, requires Rust toolchain; install separately if needed
# intuitlib not on PyPI — OAuth handled natively via httpx

# Background tasks
celery==5.6.3
redis==5.3.1

# Auth & Security
python-jose[cryptography]==3.5.0   # 3.3.0: CVE-2024-33663 / CVE-2024-33664 (JWT bomb)
passlib[bcrypt]==1.7.4
pyotp==2.10.0
cryptography==43.0.3

# HTTP & OAuth
httpx==0.28.1
authlib==1.8.0
python-multipart==0.0.32           # 0.0.12: CVE-2024-53981 (boundary DoS)

# Email
sendgrid==6.12.5

# Billing (billing.py runs in MOCK mode if this is missing)
stripe==11.6.0

# File processing
pypdf==5.9.0                       # replaces unmaintained pypdf2 3.0.1 (CVE-2023-36464)
openpyxl==3.1.5
pandas==2.3.3
watchfiles==0.24.0

# Browser automation (headless portal workflows)
# Run `playwright install chromium` after pip install
playwright==1.62.0

# Google APIs (Gmail + Sheets)
google-auth==2.58.0
google-auth-oauthlib==1.4.1
google-api-python-client==2.200.0

# Computer Use — desktop-only; install in Tauri companion process, NOT the cloud API
# pyautogui==0.9.54
# mss==9.0.2
Pillow==11.3.0              # Image processing (used server-side for resize too)
# pytesseract==0.3.13       # OCR — optional, requires system tesseract

# Config
python-dotenv==1.2.3
pydantic==2.13.5
pydantic-settings==2.15.0

# Monitoring
sentry-sdk[fastapi]==2.69.1
python-json-logger==2.0.7

# Test dependencies live in requirements-test.txt (pytest, pytest-asyncio,
# anyio[trio]) so the production image doesn't carry them and there is exactly
# one pin per package. httpx is a runtime dependency, pinned once above.
