"""SecretaryAI backend package."""

# Single source of truth for the release version across the backend
# (FastAPI app metadata, /health response, and any other version reporting).
# Keep this in sync with the desktop/frontend release tag at build time.
__version__ = "1.0.0"
