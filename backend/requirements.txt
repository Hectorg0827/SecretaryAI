# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.0

# Database
supabase==2.7.0
asyncpg==0.29.0
sqlalchemy==2.0.36
alembic==1.13.3

# AI
anthropic==0.40.0

# QuickBooks
conductor-api==0.0.1
python-quickbooks==0.9.7
intuitlib==1.3.0

# Background tasks
celery==5.4.0
redis==5.1.0

# Auth & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pyotp==2.9.0
cryptography==43.0.3

# HTTP & OAuth
httpx==0.27.2
authlib==1.3.2
python-multipart==0.0.12

# Email
sendgrid==6.11.0

# File processing
pypdf2==3.0.1
openpyxl==3.1.5
pandas==2.2.3

# Google APIs (Gmail + Sheets)
google-auth==2.36.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.153.0

# Computer Use — screen capture & control
# NOTE: These run on the desktop (via Tauri IPC), not the cloud server.
# Install only in the desktop companion process, not the cloud API.
pyautogui==0.9.54           # Mouse/keyboard control
mss==9.0.2                  # Fast cross-platform screen capture
Pillow==11.0.0              # Image processing
pytesseract==0.3.13         # OCR for PII detection (optional)

# Config
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.0

# Monitoring
sentry-sdk[fastapi]==2.18.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
