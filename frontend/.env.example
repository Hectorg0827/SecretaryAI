# SecretaryAI Frontend — Environment Variables
# In development: copy to .env.local
# In Docker production: nginx proxies /api and /auth, so leave VITE_API_URL empty

# API base URL — empty = same origin (nginx proxies to backend)
# Set to http://localhost:8000 for local dev without Docker
VITE_API_URL=
