# Test-only dependencies. LAYERS ON TOP OF requirements.txt — install both:
#
#     pip install -r requirements.txt -r requirements-test.txt
#
# Nothing that is already pinned in requirements.txt may be repeated here.
# This file previously duplicated 14 runtime pins (fastapi, python-jose,
# python-multipart, pydantic, httpx, …). When requirements.txt was bumped for
# the CVE fixes in Pass 3, the copies here still pinned the OLD versions, so
# pip hit `ResolutionImpossible: Cannot install fastapi==0.115.0 and
# fastapi==0.115.6` and CI's "Install deps" step failed — and before that, the
# duplicates silently held the test run on the vulnerable python-jose 3.3.0 /
# python-multipart 0.0.12. One pin, one place.

pytest==8.4.2
pytest-asyncio==0.26.0
anyio[trio]==4.15.1
